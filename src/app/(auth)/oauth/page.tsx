export default function OAuth() {
  //   useEffect(() => {
  //     if (accessToken) {
  //       signIn('google', { callbackUrl: '/your-redirect-url', accessToken });
  //     }
  //   }, [accessToken]);
}
